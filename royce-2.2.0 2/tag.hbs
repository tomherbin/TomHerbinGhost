{{!< default}}
{{!-- The tag above means - insert everything in this file into the {body} of the default.hbs template --}}

{{!-- The content area on the tag archive page --}}
<main id="main" class="site-main">

	<div class="post-feed inner">
		{{!-- Everything inside the #tag tags pulls data from the tag --}}
		{{#tag}}
		<header class="page-header underline">
			<h1 class="page-title">{{name}}</h1>
			<p class="page-description">{{plural ../pagination.total empty=(t "A collection of 0 posts") singular=(t "A collection of % post") plural=(t "A collection of % posts")}}</p>
		</header><!-- .page-header -->
		{{/tag}}
		{{!-- The tag below includes the post loop - partials/loop-archive.hbs --}}
		{{> "loop-archive"}}
	</div><!-- .post-feed -->

	{{!-- The tag below includes the subscribe section - partials/subscribe-box.hbs --}}
	{{#if @labs.members}}
		{{#unless @member}}
			{{> "subscribe-box"}}
		{{/unless}}
	{{/if}}

</main><!-- .site-main -->
